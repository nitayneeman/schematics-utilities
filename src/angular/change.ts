import {
  Host,
  Change,
  NoopChange,
  InsertChange,
  RemoveChange,
  ReplaceChange
} from '@schematics/angular/utility/change';

export { Host, Change, NoopChange, InsertChange, RemoveChange, ReplaceChange };
