import {
  htmlSelectorRe,
  validateName,
  validateHtmlSelector,
  validateProjectName
} from '@schematics/angular/utility/validation';

export { htmlSelectorRe, validateName, validateHtmlSelector, validateProjectName };
