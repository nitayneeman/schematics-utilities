import { getSourceFile, addModuleImportToRootModule, addModuleImportToModule } from '@angular/cdk/schematics';

export { getSourceFile, addModuleImportToRootModule, addModuleImportToModule };

// TODO: Remove after finishing the migration completely
export function getIndexHtmlPath(): any {}

// TODO: Remove after finishing the migration completely
export function getStylesPath(): any {}
